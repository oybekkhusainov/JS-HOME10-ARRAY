function makeNegative() {
let c ;

    let number = prompt("Enter a number");

    if (number > 0) {
        c = number * -1;
    }
    else{
        c = number* -1;
    }
    return c;
}
document.write(makeNegative());